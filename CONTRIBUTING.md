# Contributing

Use short-lived branches, Conventional Commits, tests alongside behavior, and update configuration documentation. Run `scripts/check.sh` before review. Never commit credentials, `.env`, volumes, private content, or provider responses.
